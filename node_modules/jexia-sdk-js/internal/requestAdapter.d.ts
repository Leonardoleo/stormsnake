import { Observable } from "rxjs";
import { Logger } from "../api/logger/logger";
import { Fetch, IRequestOptions } from "./requestAdapter.interfaces";
export declare class RequestAdapter {
    private fetch;
    private logger;
    constructor(fetch: Fetch);
    provideLogger(logger: Logger): void;
    execute<T = any>(uri: string, opt: IRequestOptions): Observable<T>;
    upload<T = any>(uri: string, headers: {
        [header: string]: string;
    }, body: any): Observable<T>;
    private handleResponse;
    private fetchErrorMessage;
}
export * from "./requestAdapter.interfaces";

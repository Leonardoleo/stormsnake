import { RequestExecuter } from "../../../internal/executer";
import { ResourceType } from "../resource";
import { FilterableQuery } from "./filterableQuery";
export declare class UpdateQuery<T> extends FilterableQuery<T> {
    protected readonly body: Partial<T>;
    constructor(queryExecuter: RequestExecuter, body: Partial<T>, resourceType: ResourceType, resourceName: string);
}

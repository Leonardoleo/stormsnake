import { RequestExecuter } from "../../../internal/executer";
import { ResourceType } from "../resource";
import { BaseQuery } from "./baseQuery";
export declare class InsertQuery<T> extends BaseQuery<T> {
    protected readonly body: Array<Partial<T>>;
    constructor(queryExecuter: RequestExecuter, body: Array<Partial<T>>, resourceType: ResourceType, resourceName: string);
}

import { Observable } from "rxjs";
import { IAuthOptions, TokenManager } from "../api/core/tokenManager";
import { IRequestExecuterData } from "./executer.interfaces";
import { RequestAdapter } from "./requestAdapter";
export declare class RequestExecuter {
    private config;
    private systemInit;
    private requestAdapter;
    private tokenManager;
    constructor(config: IAuthOptions, systemInit: Promise<any>, requestAdapter: RequestAdapter, tokenManager: TokenManager);
    executeRequest<T = any>(request: IRequestExecuterData): Observable<T>;
    private getRequestOptions;
    private hasBody;
    private parseQueryParams;
    private getUrl;
}

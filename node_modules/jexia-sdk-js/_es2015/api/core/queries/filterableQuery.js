import { BaseQuery } from "./baseQuery";
export class FilterableQuery extends BaseQuery {
    constructor() {
        super(...arguments);
        this.body = null;
    }
    where(filter) {
        this.query.setFilterCriteria(filter);
        return this;
    }
}

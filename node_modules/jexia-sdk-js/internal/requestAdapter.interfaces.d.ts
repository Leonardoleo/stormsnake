export declare enum RequestMethod {
    GET = "GET",
    POST = "POST",
    PUT = "PUT",
    PATCH = "PATCH",
    DELETE = "DELETE",
    OPTIONS = "OPTIONS"
}
export interface IRequestOptions extends RequestInit {
    body?: any;
}
export interface IHTTPResponse {
    ok: boolean;
    status: number;
    statusText: string;
    text(): Promise<string>;
}
export declare type Fetch = (url: string, init?: IRequestOptions) => Promise<IHTTPResponse>;
export interface IRequestError {
    id?: string;
    request: IRequestOptions;
    httpStatus: {
        code: number;
        status: string;
    };
    message?: string;
}

import { Observable } from "rxjs";
import { RequestAdapter } from "../../internal/requestAdapter";
import { IAuthOptions, TokenManager } from "../core/tokenManager";
import { FilesetInterface, FilesetMultipart, IFormData } from "./fileops.interfaces";
export declare class FileUploader<FormDataType extends IFormData<F>, T, F> {
    private config;
    private filesetName;
    private tokenManager;
    private requestAdapter;
    private formData;
    constructor(config: IAuthOptions, filesetName: string, tokenManager: TokenManager, requestAdapter: RequestAdapter);
    provideFormData(formData: FormDataType): void;
    upload(files: Array<FilesetMultipart<T, F>>): Observable<FilesetInterface<T>>;
    private uploadFile;
    private execute;
    private getUrl;
    private resetFormData;
}

import { InjectionToken } from "injection-js";
import { Fetch } from "../../internal/requestAdapter";
import { IModule } from "./module";
import { IAuthOptions } from "./tokenManager";
export declare const ClientInit: InjectionToken<Promise<Client>>;
export declare const ClientConfiguration: InjectionToken<unknown>;
export declare class Client {
    private fetch;
    private tokenManager;
    private modules;
    constructor(fetch: Fetch);
    init(opts: IAuthOptions, ...modules: IModule[]): Promise<Client>;
    terminate(): Promise<Client>;
    private collectConfiguration;
}

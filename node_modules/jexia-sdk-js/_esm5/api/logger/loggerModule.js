import { Logger } from "./logger";
var LoggerModule = (function () {
    function LoggerModule(level, module) {
        this.level = level;
        this.module = module;
    }
    LoggerModule.prototype.init = function (coreInjector) {
        var logger = coreInjector.get(Logger);
        logger.config(this.level, this.module);
        return Promise.resolve(this);
    };
    LoggerModule.prototype.getConfig = function () {
        return {
            logger: {
                level: this.level,
                module: this.module,
            }
        };
    };
    LoggerModule.prototype.terminate = function () {
        return Promise.resolve(this);
    };
    return LoggerModule;
}());
export { LoggerModule };
